% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clustermap.R
\name{clustermap}
\alias{clustermap}
\title{Clustered heatmap with dendrograms}
\usage{
clustermap(
  mat,
  metric = c("euclidean", "correlation"),
  linkage = c("average", "complete", "ward"),
  colormap = c("viridis", "rdbu"),
  z_score = FALSE,
  cluster_rows = TRUE,
  cluster_cols = TRUE,
  show_row_dendrogram = TRUE,
  show_col_dendrogram = TRUE,
  show_labels = TRUE,
  legend_title = "value",
  row_linkage = NULL,
  col_linkage = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{mat}{A numeric matrix. Row and column names, if present, are used as
labels. Values are transported row-major to the browser.}

\item{metric}{Distance metric for clustering: \code{"euclidean"} or
\code{"correlation"} (1 - Pearson correlation).}

\item{linkage}{Agglomeration method: \code{"average"}, \code{"complete"} or \code{"ward"}.}

\item{colormap}{Color ramp: \code{"viridis"} (sequential) or \code{"rdbu"} (diverging).}

\item{z_score}{Standardize each row to mean 0 / sd 1 before coloring.}

\item{cluster_rows, cluster_cols}{Cluster and reorder rows / columns. Ignored
for an axis when a precomputed \code{row_linkage} / \code{col_linkage} is supplied.}

\item{show_row_dendrogram, show_col_dendrogram}{Draw the row / column
dendrogram.}

\item{show_labels}{Draw row/column tick labels (auto-hidden when cells get
too small to be legible).}

\item{legend_title}{Colorbar legend title.}

\item{row_linkage, col_linkage}{Optional precomputed leaf order or dendrogram
to skip clustering that axis. Either an integer vector giving the 0-based
leaf order, or a list with \code{order} (0-based) and \code{merges} (each a list with
\code{left}, \code{right}, \code{height}; leaves are \verb{0..n-1}, internal node \code{k} is
\code{n + k}).}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A hierarchically-clustered expression heatmap (in the spirit of
\code{seaborn.clustermap} / Morpheus): the matrix is drawn on a GPU/canvas data
layer so large matrices stay smooth, while dendrograms, tick labels and the
colorbar are crisp vector overlays. Rows and columns are agglomeratively
clustered and reordered so structure appears as blocks along the diagonal.
}
\details{
Clustering is at least \code{O(n^2)} in the number of rows/columns (it builds a
full distance matrix), so \code{clustermap()} only clusters automatically when a
dimension has at most 2000 leaves. For larger matrices, precompute a leaf
order (or a dendrogram) elsewhere and pass it via \code{row_linkage} /
\code{col_linkage} to skip clustering; the heatmap rendering itself scales to
much larger matrices.
}
\examples{
set.seed(1)
# Two clear blocks of correlated genes across two groups of samples.
mat <- rbind(
  matrix(rnorm(20 * 10, mean = 2), nrow = 20),
  matrix(rnorm(20 * 10, mean = -2), nrow = 20)
)
rownames(mat) <- paste0("gene", seq_len(nrow(mat)))
colnames(mat) <- paste0("s", seq_len(ncol(mat)))
clustermap(mat, colormap = "rdbu", z_score = TRUE)
}
