% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/heatmap.R
\name{bioheatmap}
\alias{bioheatmap}
\alias{heatmap_bioviz}
\title{Expression heatmap}
\usage{
bioheatmap(
  mat,
  colormap = c("viridis", "rdbu"),
  z_score = FALSE,
  vmin = NULL,
  vmax = NULL,
  show_colorbar = TRUE,
  width = NULL,
  height = NULL,
  element_id = NULL
)

heatmap_bioviz(
  mat,
  colormap = c("viridis", "rdbu"),
  z_score = FALSE,
  vmin = NULL,
  vmax = NULL,
  show_colorbar = TRUE,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{mat}{A numeric matrix (rows x columns). \code{rownames(mat)} and
\code{colnames(mat)}, when present, are used as row/column tick labels.}

\item{colormap}{Color ramp: \code{"viridis"} (sequential) or \code{"rdbu"} (diverging).}

\item{z_score}{Logical; if \code{TRUE}, each row is z-score normalized before
coloring (a row-centered heatmap).}

\item{vmin, vmax}{Lower/upper clamp of the color domain. \code{NULL} (the default)
auto-scales from the data; for \code{"rdbu"} the auto domain is symmetric about
zero.}

\item{show_colorbar}{Logical; draw the colorbar legend.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A GPU-accelerated heatmap for large expression matrices (samples x genes).
The matrix is uploaded to the GPU as a single texture and colormapped in a
fragment shader (via \code{regl}), so matrices with a million or more cells pan
and zoom smoothly. The colorbar legend and row/column tick labels are drawn
as crisp vector overlays; ticks appear only when few enough to be legible.
}
\details{
The function is exported as \code{bioheatmap()} (and aliased as \code{heatmap_bioviz()})
to avoid masking \code{\link[stats:heatmap]{stats::heatmap()}}.
}
\examples{
set.seed(1)
m <- matrix(rnorm(50 * 30), nrow = 50, ncol = 30)
rownames(m) <- paste0("gene", seq_len(50))
colnames(m) <- paste0("sample", seq_len(30))
bioheatmap(m, z_score = TRUE)
}
