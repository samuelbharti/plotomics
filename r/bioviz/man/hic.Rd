% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hic.R
\name{hic}
\alias{hic}
\title{Hi-C contact matrix}
\usage{
hic(
  mat,
  n = NULL,
  bin_size = NULL,
  chrom = NULL,
  colormap = "viridis",
  transform = c("log", "linear"),
  vmax = NULL,
  vmin = 0,
  symmetric = TRUE,
  label = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{mat}{Either a square numeric matrix of contact counts, or a data frame
/ list giving a sparse COO triplet with integer columns \code{i}, \code{j} and a
numeric \code{v}. When a triplet is supplied, \code{n} must be given (or inferable
from \code{max(i, j) + 1}).}

\item{n}{Number of bins per axis. Required for the sparse (i/j/v) form;
ignored for a dense matrix (taken from \code{nrow(mat)}).}

\item{bin_size}{Genomic bin size in base pairs; used to label axes in
bp/kb/Mb. \code{NULL} labels axes by bin index.}

\item{chrom}{Optional chromosome name shown as the axis title.}

\item{colormap}{Sequential colormap for intensity (currently \code{"viridis"}).}

\item{transform}{Intensity transform, \code{"log"} (default) or \code{"linear"}.}

\item{vmax}{Upper clip of the intensity scale; \code{NULL} auto-picks a high
percentile.}

\item{vmin}{Lower clip of the intensity scale.}

\item{symmetric}{Mirror sparse \code{i}/\code{j}/\code{v} entries across the diagonal.}

\item{label}{Axis title (overrides \code{chrom} when set).}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A GPU-accelerated Hi-C chromatin contact map. The matrix is uploaded once as
a single-channel float texture and drawn as one WebGL quad (via \code{regl}), with
the colormap and log/linear transform applied in the fragment shader, so
pan/zoom stays smooth on very large matrices. A precomputed level-of-detail
pyramid keeps interaction fast when zoomed out. Axes (genomic coordinate
ticks) and the colorbar are drawn as crisp vector overlays. No tile server is
required.
}
\examples{
set.seed(1)
n <- 200
# distance-decay background contact matrix
d <- abs(outer(seq_len(n), seq_len(n), `-`))
m <- 1000 / (d + 1)^1.2 + matrix(runif(n * n), n, n)
m <- (m + t(m)) / 2 # symmetrize
hic(m, bin_size = 10000, chrom = "chr1")
}
