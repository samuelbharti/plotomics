% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/igv.R
\name{igv}
\alias{igv}
\title{Genome viewer (igv.js)}
\usage{
igv(
  genome = "hg38",
  locus = NULL,
  tracks = list(),
  config = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{genome}{Genome identifier understood by igv.js (e.g. \code{"hg38"},
\code{"hg19"}, \code{"mm10"}). Used when \code{config} is \code{NULL}.}

\item{locus}{Optional initial locus string, e.g.
\code{"chr8:127,736,588-127,739,371"}, or a gene symbol.}

\item{tracks}{A list of igv.js track configurations (each a named list with
at least a \code{url}). Used when \code{config} is \code{NULL}.}

\item{config}{Optional full igv.js browser configuration (named list). When
supplied it is passed through as-is and the convenience arguments are
ignored.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
An embeddable interactive genome browser powered by
\href{https://github.com/igvteam/igv.js}{igv.js}. Unlike the other plotomics
components this one is \emph{config-driven}: igv.js streams and tiles remote
indexed files (BAM/CRAM, bigWig, VCF, BED, ...) itself, so data flows through
the browser configuration as URLs rather than as columns.
}
\details{
Supply either a full igv.js \code{config} (a named list matching the igv.js
browser configuration) or the convenience arguments \code{genome}, \code{locus} and
\code{tracks}, which are assembled into a config when \code{config} is \code{NULL}.
}
\examples{
# Genome only
igv(genome = "hg38")

# Genome + locus + a public bigWig track
igv(
  genome = "hg38",
  locus = "chr8:127,736,588-127,739,371",
  tracks = list(list(
    name = "CTCF",
    url = "https://www.encodeproject.org/files/ENCFF356YES/@download/ENCFF356YES.bigWig",
    format = "bigWig"
  ))
)
}
