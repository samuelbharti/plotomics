% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{network}
\alias{network}
\title{Network graph}
\usage{
network(
  nodes,
  edges,
  layout = c("forceatlas2", "precomputed"),
  iterations = 200,
  default_node_color = "#7c8598",
  default_edge_color = "#d6dae1",
  label_threshold = 8,
  default_node_size = 4,
  palette = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{nodes}{A data frame of nodes. Must contain an \code{id} column (coerced to
character). Optional columns: \code{x}, \code{y} (precomputed coordinates), \code{size}
(node radius in px), \code{group} (categorical, mapped to a palette color) and
\code{label} (display name; defaults to \code{id}).}

\item{edges}{A data frame of edges with columns \code{source} and \code{target} holding
node ids, and an optional numeric \code{weight} column.}

\item{layout}{Either \code{"forceatlas2"} (run a layout when coordinates are
missing) or \code{"precomputed"} (require and use \code{x}/\code{y} from \code{nodes}).}

\item{iterations}{Number of ForceAtlas2 iterations (bounded internally).}

\item{default_node_color}{Fallback node color for nodes without a \code{group}.}

\item{default_edge_color}{Edge color.}

\item{label_threshold}{Minimum node size (px) for its label to render.}

\item{default_node_size}{Node radius (px) used when \code{size} is absent.}

\item{palette}{Optional character vector of hex colors overriding the default
categorical palette used for node groups.}

\item{theme}{Optional named list of theme overrides (colors, fonts, ...)
merged over the component defaults in the browser. \code{NULL} uses the default
theme.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A GPU-accelerated node-link diagram for large gene/protein interaction
networks. Nodes and edges are rendered with WebGL (via sigma v3 over a
graphology graph) so tens of thousands of elements stay interactive. When
node coordinates are not supplied, a bounded ForceAtlas2 layout positions the
nodes in the browser; otherwise the supplied \code{x}/\code{y} are used. Categorical
node groups are colored from a colorblind-safe palette. Hovering a node
highlights it and its neighbors; zoom and pan use sigma's camera.
}
\examples{
set.seed(1)
nodes <- data.frame(
  id = paste0("N", 1:60),
  group = sample(c("A", "B", "C"), 60, replace = TRUE)
)
edges <- data.frame(
  source = paste0("N", sample(1:60, 120, replace = TRUE)),
  target = paste0("N", sample(1:60, 120, replace = TRUE))
)
network(nodes, edges)
}
