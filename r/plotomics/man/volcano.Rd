% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/volcano.R
\name{volcano}
\alias{volcano}
\title{Volcano plot}
\usage{
volcano(
  data,
  fc_threshold = 1,
  p_threshold = 0.05,
  point_size = 3,
  opacity = 0.8,
  colors = NULL,
  x_label = "log2 fold change",
  y_label = "-log10 p-value",
  show_threshold_lines = TRUE,
  label_top_n = 10,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with numeric columns \code{x} (log2 fold change) and \code{y}
(\code{-log10} p-value). An optional \code{label} column supplies gene names for
tooltips and top-hit labels.}

\item{fc_threshold}{Absolute log2 fold-change cutoff for calling a hit.}

\item{p_threshold}{P-value cutoff (applied on the \code{-log10} scale).}

\item{point_size}{Point radius in pixels.}

\item{opacity}{Point opacity in \verb{[0, 1]}.}

\item{colors}{Optional named list of hex colors for the three point classes:
\code{up}, \code{down} and \code{ns} (not significant). \code{NULL} (the default) uses the
component's built-in palette.}

\item{x_label, y_label}{Axis titles.}

\item{show_threshold_lines}{Draw the fold-change / p-value threshold guides.}

\item{label_top_n}{Number of top up- and down-regulated genes to label.}

\item{theme}{Optional named list of theme overrides (colors, fonts, ...)
merged over the component defaults in the browser. \code{NULL} uses the default
theme.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A GPU-accelerated volcano plot for differential-expression results. Points
are rendered with WebGL (via regl-scatterplot) so hundreds of thousands to
millions of genes/features stay interactive, while axes, threshold guides
and gene labels are drawn as crisp vector overlays.
}
\examples{
set.seed(1)
df <- data.frame(
  x = rnorm(2000),
  y = abs(rnorm(2000)) * 3,
  label = paste0("GENE", seq_len(2000))
)
volcano(df)
}
