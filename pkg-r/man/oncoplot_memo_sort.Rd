% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oncoplot.R
\name{oncoplot_memo_sort}
\alias{oncoplot_memo_sort}
\title{Conventional oncoplot row and column ordering}
\usage{
oncoplot_memo_sort(alterations, genes = NULL, samples = NULL)
}
\arguments{
\item{alterations}{A data frame with columns \code{gene}, \code{sample} and \code{class}.}

\item{genes}{Optional character vector to use instead of the derived gene
order.}

\item{samples}{Optional character vector to use instead of the derived sample
order.}
}
\value{
A list with \code{genes} and \code{samples} character vectors.
}
\description{
Genes are ordered by descending alteration frequency, then samples are
ordered so that the most frequently altered gene's carriers come first, ties
broken by the next gene down. This is the "memo sort" cBioPortal popularised;
it is what makes mutual exclusivity between drivers visible as a staircase.
}
\details{
Exposed separately so a caller can compute the order once and reuse it for
both an interactive \code{\link[=oncoplot]{oncoplot()}} and a static rendering, rather than letting
two implementations tie-break differently.
}
