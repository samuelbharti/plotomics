% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upset.R
\name{upset_intersections}
\alias{upset_intersections}
\title{Exclusive set intersections from a membership matrix}
\usage{
upset_intersections(m, max_n = NULL)
}
\arguments{
\item{m}{A logical matrix, elements x sets, with set names as column names.}

\item{max_n}{Keep only the \code{max_n} largest intersections. \code{NULL} keeps all.}
}
\value{
A list with \code{size} (integer vector), \code{membership} (logical matrix,
intersections x sets), \code{sets}, \code{set_sizes} and \code{total}.
}
\description{
Counts the elements belonging to precisely each observed combination of sets.
Elements in no set are excluded, since they have no column to sit in.
}
\examples{
m <- cbind(A = c(TRUE, TRUE, FALSE), B = c(TRUE, FALSE, TRUE))
upset_intersections(m)
}
