% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/profile.R
\name{bioprofile}
\alias{bioprofile}
\alias{profile_plotomics}
\title{Grouped categorical bar profile}
\usage{
bioprofile(
  data,
  groups = NULL,
  group_colors = NULL,
  title = NULL,
  bar_width = 0.62,
  as_fraction = FALSE,
  show_header = TRUE,
  show_bar_labels = TRUE,
  y_label = "mutations",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)

profile_plotomics(
  data,
  groups = NULL,
  group_colors = NULL,
  title = NULL,
  bar_width = 0.62,
  as_fraction = FALSE,
  show_header = TRUE,
  show_bar_labels = TRUE,
  y_label = "mutations",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with a numeric \code{value} column. Optional \code{group}
(category per bar, whose contiguous runs become the header blocks) and
\code{label} (per-bar tick label) columns.}

\item{groups}{Character vector fixing the group order and colour assignment.
Defaults to order of appearance.}

\item{group_colors}{One hex colour per group. \code{NULL} uses the component's
categorical palette.}

\item{title}{Optional title drawn above the header band.}

\item{bar_width}{Fraction of each slot the bar occupies, in \verb{(0, 1]}.}

\item{as_fraction}{Show values as a share of the total rather than raw counts.}

\item{show_header, show_bar_labels}{Toggle the header band and tick labels.}

\item{y_label}{Axis label.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
An ordered bar profile whose categories collapse into coloured header blocks.
Built for the 96-context mutational signature plot, where the bars are the
trinucleotide contexts and the six blocks are the substitution classes, a
layout conventional enough that readers parse it without a legend. It
generalises to any ordered categorical profile that groups into runs, hence
the generic name.
}
\details{
Bars are canvas-drawn, so a few thousand bins (a binned copy-number profile,
a coverage track) work as well as 96 contexts.

Bars are drawn in the order given. For SBS96 that order is part of the
convention, so the component does not sort.

Named \code{bioprofile()} rather than \code{profile()} so that attaching the package
does not mask the \code{\link[stats:profile]{stats::profile()}} generic, which profiles a fitted
model's likelihood. This follows \code{\link[=bioheatmap]{bioheatmap()}}, which keeps clear of
\code{\link[stats:heatmap]{stats::heatmap()}} the same way. \code{profile_plotomics()} is an alias, for
symmetry with \code{heatmap_plotomics()}.
}
\examples{
df <- data.frame(
  value = c(3, 5, 2, 8),
  group = c("C>A", "C>A", "C>T", "C>T"),
  label = c("ACA", "ACC", "TCA", "TCT")
)
bioprofile(df)
}
