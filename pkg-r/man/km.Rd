% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km.R
\name{km}
\alias{km}
\title{Kaplan-Meier survival curves with a number-at-risk table}
\usage{
km(
  data,
  groups = NULL,
  group_colors = NULL,
  risk_times = NULL,
  risk_counts = NULL,
  p_label = NULL,
  show_ci = TRUE,
  show_censors = TRUE,
  show_risk_table = TRUE,
  show_legend = TRUE,
  y_from_zero = TRUE,
  line_width = 2,
  x_label = "months",
  y_label = "overall survival",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{Either a \code{survival::survfit} object, or a data frame with numeric
\code{time} and \code{surv} columns and optional \code{lower}, \code{upper} and \code{group}
columns. Within a stratum, rows must be in ascending time order.}

\item{groups}{Character vector fixing the stratum order and colour
assignment. Defaults to order of appearance.}

\item{group_colors}{One hex colour per stratum. \code{NULL} uses the component's
categorical palette.}

\item{risk_times}{Numeric vector of times for the at-risk table, also used as
the x-axis ticks. \code{NULL} picks an even grid across the follow-up.}

\item{risk_counts}{Integer matrix, strata x \code{risk_times}. Computed from a
\code{survfit} object automatically; required alongside \code{risk_times} when you
pass a data frame and want the table.}

\item{p_label}{Optional annotation drawn inside the panel, e.g.
\code{"log-rank p = 0.02"}. Not computed here: pass what your test returned.}

\item{show_ci, show_censors, show_risk_table, show_legend}{Toggle the confidence
band, censoring ticks, at-risk table and legend.}

\item{y_from_zero}{Start the y axis at zero. Opt-out rather than automatic:
zooming y exaggerates separation between curves.}

\item{line_width}{Curve stroke width in pixels.}

\item{x_label, y_label}{Axis titles.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A right-continuous step curve per stratum, censoring ticks, an optional
pointwise confidence band, and the number-at-risk table underneath. The table
is on by default because a survival curve without one hides how much of its
tail rests on a handful of patients, which is where readers over-read it.
}
\details{
The widget draws, it does not estimate. Pass a \code{survfit} object and the
estimates are read off it; pass a data frame and they are used as given. Both
routes mean the numbers on screen are the ones your model produced, so a
figure rendered here and the same figure rendered by \code{plot()} cannot disagree
about where a curve steps.
}
\examples{
df <- data.frame(
  time = c(0, 5, 12, 0, 7, 15),
  surv = c(1, 0.9, 0.7, 1, 0.8, 0.5),
  group = rep(c("treated", "control"), each = 3)
)
km(df)
}
