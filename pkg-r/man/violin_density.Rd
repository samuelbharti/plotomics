% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/violin.R
\name{violin_density}
\alias{violin_density}
\title{Kernel densities on a shared grid}
\usage{
violin_density(values, n = 64L, adjust = 1)
}
\arguments{
\item{values}{A named list of numeric vectors, one per violin. Names of the
form \code{"feature|group"} are split into the two key columns.}

\item{n}{Grid resolution.}

\item{adjust}{Bandwidth multiplier, passed to \code{\link[stats:density]{stats::density()}}.}
}
\value{
A list with \code{data} (the key columns), \code{grid}, \code{density} and \code{median}.
}
\description{
Evaluates every group's density on one grid spanning all of them, which is
what lets the violins be compared. Groups with fewer than two distinct values
get a flat zero row rather than an error, since a cluster with one cell is a
real thing to encounter.
}
\examples{
violin_density(list(`A|x` = rnorm(20), `A|y` = rnorm(20, 3)))
}
