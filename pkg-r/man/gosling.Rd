% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gosling.R
\name{gosling}
\alias{gosling}
\title{Declarative genomics figures (Gosling.js)}
\usage{
gosling(
  spec,
  padding = NULL,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{spec}{A Gosling specification as a (nested) named list. It is passed
through verbatim to Gosling.js after JSON serialization, so option keys use
Gosling's own camelCase names (e.g. \code{tracks}, \code{xDomain}, \code{alignment}). Must
contain at least one of \code{tracks}, \code{views}, \code{arrangement}, \code{alignment} or
\code{template}.}

\item{padding}{Optional outer padding (pixels) forwarded to Gosling's embed
options.}

\item{theme}{Optional Gosling theme: a built-in name (e.g. \code{"dark"}) or a
theme list.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
A config-driven genome-visualization widget powered by
\href{https://gosling-lang.org}{Gosling.js}, the grammar of scalable, linked,
interactive nucleotide graphics. Rendering is fully declarative: you pass a
Gosling \emph{specification} (an R list serialized to JSON) and data flows through
the spec's own \code{data} blocks (tileset URLs, indexed BAM/BED/VCF/BigWig, or
CSV/JSON URLs and inline values). Gosling streams and tiles large genomic
datasets on the GPU via HiGlass, so no separate \code{data} argument is needed.
}
\examples{
spec <- list(
  title = "Example track",
  tracks = list(list(
    data = list(
      url = paste0(
        "https://server.gosling-lang.org/api/v1/tileset_info/",
        "?d=cistrome-multivec"
      ),
      type = "multivec",
      row = "sample",
      column = "position",
      value = "peak",
      categories = list("sample 1")
    ),
    mark = "bar",
    x = list(field = "start", type = "genomic"),
    xe = list(field = "end", type = "genomic"),
    y = list(field = "peak", type = "quantitative"),
    width = 700, height = 200
  ))
)
gosling(spec)
}
