% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial}
\alias{spatial}
\title{Spatial map over a tissue image}
\usage{
spatial(
  data,
  image,
  img_width,
  img_height,
  spot_diameter = 4,
  levels = NULL,
  colors = NULL,
  color_mode = c("auto", "categorical", "continuous"),
  colormap = c("viridis", "rdbu", "ltc", "ltcdiv"),
  spot_scale = 1,
  spot_opacity = 0.85,
  image_opacity = 1,
  show_image = TRUE,
  show_legend = TRUE,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with numeric \code{x} and \code{y} columns giving spot centres
\strong{in image pixel coordinates}. Optional \code{color} (character or numeric) and
\code{label} (tooltip text) columns.}

\item{image}{URL or path of the tissue image, as the browser will fetch it.}

\item{img_width, img_height}{Natural size of that image in pixels.}

\item{spot_diameter}{Spot diameter in image pixels.}

\item{levels, colors}{Character vectors fixing the categorical order and
colours. \code{NULL} derives them from the data and the theme palette.}

\item{color_mode}{\code{"auto"} (detect from the column type), \code{"categorical"} or
\code{"continuous"}.}

\item{colormap}{Sequential ramp for continuous colouring: \code{"viridis"},
\code{"rdbu"}, \code{"ltc"} (an earthy teal to sand to rust sequential ramp) or
\code{"ltcdiv"} (its diverging counterpart, neutral cream at the midpoint).}

\item{spot_scale}{Multiplier on \code{spot_diameter}; 1 draws true size.}

\item{spot_opacity, image_opacity}{Opacities in \verb{[0, 1]}. Lower the spot
opacity to read the histology underneath.}

\item{show_image, show_legend}{Toggle the underlay and the legend.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
Measurements plotted at their real coordinates on a slide, drawn on top of
the histology image they came from. This is the layout of a spatial
transcriptomics experiment: for a spatial assay the tissue \emph{is} the axis, and
a cluster tracing the edge of an invasive front says something an embedding
cannot.
}
\details{
The image and the spots share one "contain" fit, computed once, so histology
and overlay cannot drift apart on resize, full-screen, or a high-DPI display.

\code{color} may be a character vector (categorical, discrete legend) or numeric
(continuous, sequential ramp with a colourbar), which is what lets one view
toggle between colouring by cluster and by a gene's expression.
}
\examples{
df <- data.frame(
  x = c(100, 150, 200), y = c(120, 160, 90),
  color = c("Cluster 1", "Cluster 2", "Cluster 1")
)
spatial(df, image = "tissue.png", img_width = 600, img_height = 600,
        spot_diameter = 8)
}
