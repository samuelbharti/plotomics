% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/upset.R
\name{upset}
\alias{upset}
\title{UpSet plot of set intersections}
\usage{
upset(
  data,
  sets,
  membership,
  set_sizes = NULL,
  total = NULL,
  bar_fraction = 0.55,
  show_set_sizes = TRUE,
  dot_radius = 5,
  bar_color = NULL,
  empty_dot_color = NULL,
  y_label = "intersection size",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with a numeric \code{size} column, one row per
intersection, in the order to draw them.}

\item{sets}{Character vector of set names, top to bottom.}

\item{membership}{Logical or 0/1 matrix, intersections x sets, saying which
sets each intersection belongs to.}

\item{set_sizes}{Numeric vector of per-set totals for the left-hand bars.}

\item{total}{Universe size, shown in the corner.}

\item{bar_fraction}{Fraction of the height given to the intersection bars.}

\item{show_set_sizes}{Draw the per-set total bars.}

\item{dot_radius}{Matrix dot radius in pixels.}

\item{bar_color, empty_dot_color}{Fills for the bars and filled dots, and for
dots not in the intersection. \code{NULL} uses the theme.}

\item{y_label}{Axis label for the intersection bars.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
Set intersections as a bar chart over a membership matrix. Venn diagrams stop
being readable at four sets and stop being drawable at five; UpSet replaces
the areas with an explicit matrix, so it scales to dozens of sets and stays
exact.
}
\details{
Intersections are \strong{exclusive}: a column counts the elements in precisely
that combination of sets and no others. That is what makes the columns sum to
the union rather than double-counting, and it is why a small \code{A + B} bar next
to large \code{A} and \code{B} bars is evidence of mutual exclusivity rather than an
artefact. \code{\link[=upset_intersections]{upset_intersections()}} computes them from a logical matrix.
}
\examples{
m <- matrix(c(TRUE, FALSE, FALSE, TRUE, TRUE, TRUE), nrow = 3, byrow = TRUE)
upset(data.frame(size = c(40, 25, 12)), sets = c("A", "B"), membership = m)
}
