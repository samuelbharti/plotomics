% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/violin.R
\name{violin}
\alias{violin}
\title{Stacked violin plot}
\usage{
violin(
  data,
  grid,
  density,
  median = NULL,
  features = NULL,
  groups = NULL,
  group_colors = NULL,
  violin_width = 0.85,
  scale_per_violin = FALSE,
  show_median = TRUE,
  show_feature_labels = TRUE,
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{data}{A data frame with \code{feature} and \code{group} key columns, one row per
violin, in the order to draw them.}

\item{grid}{Numeric vector, the shared evaluation grid, ascending.}

\item{density}{Numeric matrix, violins x \code{grid}, of density values.}

\item{median}{Optional numeric vector, one median per violin, drawn as a tick.}

\item{features, groups}{Character vectors fixing the row and column order.
Factor \code{feature} / \code{group} columns supply them from their levels.}

\item{group_colors}{One hex colour per group. \code{NULL} uses the categorical
palette.}

\item{violin_width}{Fraction of a cell's width the widest violin fills.}

\item{scale_per_violin}{Scale each violin to its own maximum rather than the
row's. Per-row is the default so groups stay comparable within a feature.}

\item{show_median, show_feature_labels}{Toggle the median tick and row labels.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
One row per feature, one violin per group. A box plot hides bimodality,
which in single-cell data is usually the whole story: a gene expressed in
half a cluster and silent in the other half has the same median as one
expressed weakly everywhere. The violin shows the shape, and stacking rows
on a shared x lets a marker panel be read down the page.
}
\details{
The widget draws densities, it does not estimate them. Each violin arrives as
a vector of density values on a shared grid, because kernel bandwidth choice
changes what the figure claims and belongs with the data. \code{\link[=violin_density]{violin_density()}}
computes them from raw values with \code{\link[stats:density]{stats::density()}}.
}
\examples{
d <- violin_density(list(`CD3D|T` = rnorm(50, 2), `CD3D|B` = rnorm(50, 0)))
violin(d$data, grid = d$grid, density = d$density, median = d$median)
}
