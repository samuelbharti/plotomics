% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/embedding.R
\name{embedding-shiny}
\alias{embedding-shiny}
\alias{embeddingOutput}
\alias{renderEmbedding}
\title{Shiny bindings for embedding}
\usage{
embeddingOutput(output_id, width = "100\%", height = "480px")

renderEmbedding(expr, env = parent.frame(), quoted = FALSE)
}
\arguments{
\item{output_id}{Output variable to read from.}

\item{width, height}{Element size, passed to
\code{\link[htmlwidgets:shinyWidgetOutput]{htmlwidgets::shinyWidgetOutput()}}.}

\item{expr}{An expression that generates an \code{\link[=embedding]{embedding()}} widget.}

\item{env}{The environment in which to evaluate \code{expr}.}

\item{quoted}{Is \code{expr} already quoted? Defaults to \code{FALSE}.}
}
\value{
\code{embeddingOutput()} returns a Shiny output UI element;
\code{renderEmbedding()} returns a Shiny render function.
}
\description{
Output and render functions for using \code{\link[=embedding]{embedding()}} within Shiny applications
and interactive R Markdown / Quarto documents.
}
\details{
In a Shiny app the lasso selection is pushed back to the server as
\verb{input$<output_id>_selected}, a 0-based integer vector of the selected rows
(so \code{embeddingOutput("umap")} populates \code{input$umap_selected}). It updates on
every completed selection and is \code{NULL} until the first one.
}
