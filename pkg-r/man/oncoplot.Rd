% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oncoplot.R
\name{oncoplot}
\alias{oncoplot}
\title{Oncoplot (OncoPrint)}
\usage{
oncoplot(
  alterations,
  genes = NULL,
  samples = NULL,
  classes = NULL,
  class_colors = NULL,
  burden = NULL,
  annotations = NULL,
  show_burden = TRUE,
  show_frequency = TRUE,
  show_annotations = TRUE,
  show_legend = TRUE,
  empty_color = "#EFE9DC",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{alterations}{A data frame of altered pairs with columns \code{gene},
\code{sample} and \code{class}. Unaltered pairs are simply absent; the full grid is
reconstructed from \code{genes} and \code{samples}.}

\item{genes}{Character vector of genes, top row first. Defaults to the genes
present in \code{alterations}, most frequently altered first.}

\item{samples}{Character vector of samples, left column first. Defaults to
the memo-sorted order.}

\item{classes}{Character vector of alteration classes, which fixes both the
legend order and the colour assignment. Defaults to the classes present.}

\item{class_colors}{Character vector of hex colours, one per entry of
\code{classes}. \code{NULL} uses the component's categorical palette.}

\item{burden}{Numeric vector, one value per sample, for the top barplot.
Defaults to the number of altered genes per sample.}

\item{annotations}{Optional list of clinical strips. Each element is a list
with \code{name} (character scalar), \code{values} (one value per sample) and an
optional \code{colors}.}

\item{show_burden, show_frequency, show_annotations, show_legend}{Toggle the
surrounding panels.}

\item{empty_color}{Fill for a gene x sample cell with no alteration.}

\item{theme}{Optional named list of theme overrides merged over the
component defaults in the browser.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
The cohort alteration landscape: a gene x sample grid of categorical
alteration classes, with a per-sample burden barplot above, a per-gene
frequency barplot to the right, and optional clinical annotation strips
below. The grid is drawn on a canvas, so cohort-scale matrices (hundreds of
genes by thousands of samples) stay interactive; labels and the legend are a
crisp vector overlay.
}
\details{
The component renders rows and columns in exactly the order it is given and
uses the burden and frequency values as supplied. Ordering an oncoplot
(memo sort, burden sort, sort by a clinical variable) is the caller's
decision, and re-deriving it in the browser would let two renderings of the
same data disagree. Use \code{\link[=oncoplot_memo_sort]{oncoplot_memo_sort()}} to get the conventional order.
}
\examples{
alt <- data.frame(
  gene = c("TP53", "TP53", "PIK3CA", "PIK3CA", "GATA3"),
  sample = c("S1", "S2", "S2", "S3", "S1"),
  class = c("Missense", "Truncating", "Missense", "Amplification", "Missense")
)
oncoplot(alt)
}
