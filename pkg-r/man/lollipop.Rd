% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lollipop.R
\name{lollipop}
\alias{lollipop}
\title{Protein domain lollipop}
\usage{
lollipop(
  variants,
  length,
  gene = NULL,
  uniprot = NULL,
  domains = NULL,
  ptms = NULL,
  classes = NULL,
  class_colors = NULL,
  domain_colors = NULL,
  label_top_n = 12,
  show_ptms = TRUE,
  show_domains = TRUE,
  show_legend = TRUE,
  min_head_radius = 3,
  max_head_radius = 11,
  y_label = "samples",
  backbone_color = "#E6DCC8",
  stem_color = "#93a1b8",
  theme = NULL,
  width = NULL,
  height = NULL,
  element_id = NULL
)
}
\arguments{
\item{variants}{A data frame with columns \code{position} (amino-acid position,
1-based) and \code{count} (recurrence). Optional \code{class} (variant class) and
\code{label} (e.g. \code{"R175H"}) columns drive the colour and the text labels.}

\item{length}{Protein length in residues.}

\item{gene, uniprot}{Identifiers shown on the axis title.}

\item{domains}{Optional data frame of domain rectangles with columns \code{name},
\code{start} and \code{end}.}

\item{ptms}{Optional data frame of modification sites with columns
\code{position} and \code{type}.}

\item{classes}{Character vector fixing the legend order and colour
assignment. Defaults to the classes present, most frequent first.}

\item{class_colors, domain_colors}{Character vectors of hex colours. \code{NULL}
uses the component's categorical palette.}

\item{label_top_n}{Label the \code{n} most recurrent variants. Which stems get a
label is resolved here and sent to the browser, so a redraw, an export and
any static counterpart all label the same ones.}

\item{show_ptms, show_domains, show_legend}{Toggle the surrounding tracks.}

\item{min_head_radius, max_head_radius}{Stem head radius range in pixels. Head
\emph{area} is proportional to recurrence, so these bound the mapping rather
than setting a size: raise \code{max_head_radius} when one hotspot dwarfs the
rest and you want the difference to read at a glance.}

\item{y_label}{Axis title for the recurrence axis.}

\item{backbone_color, stem_color}{Hex colours for the protein backbone
rectangle and the mutation stems.}

\item{theme}{Optional named list of theme overrides.}

\item{width, height}{Widget dimensions (any valid CSS size).}

\item{element_id}{Optional explicit DOM id.}
}
\value{
An \code{htmlwidget} object.
}
\description{
Variants along a protein, drawn over its domain architecture: a backbone
spanning the sequence with domain rectangles on it, mutation stems whose head
area is proportional to recurrence, and an optional post-translational
modification track below. Hotspots inside a functional domain read very
differently from truncating variants scattered across one, which is what this
figure exists to show.
}
\details{
Stems and domains are canvas-drawn so a protein with thousands of variants
stays responsive; labels, axis and legend are a vector overlay.
}
\examples{
v <- data.frame(
  position = c(175, 248, 273),
  count = c(21, 15, 13),
  class = c("Missense", "Missense", "Missense"),
  label = c("R175H", "R248Q", "R273H")
)
d <- data.frame(name = "P53 DNA-binding", start = 100, end = 288)
lollipop(v, length = 393, gene = "TP53", uniprot = "P04637", domains = d)
}
